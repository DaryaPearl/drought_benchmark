# Agro‑Drought SPI Benchmark

*Structure auto‑generated by* `scaffold_project.py`.

## Quick start
```bash
# 1. create env
pip install -r requirements.txt

# 2. run baseline model
python src/train.py model=rf
```
